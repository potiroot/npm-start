extends layout.pug

block title
  title #{title}

block content

  h1 #{title} in Pug
  if emailsVisible
    h3 Электронные адреса
    ul
      each email in emails
        li=email
  else
    h3 Электронный адрес отсутствует
  p Телефон: #{phone}

  include footer.pug